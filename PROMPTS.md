# 블랙홀을 AI에게 시키는 프롬프트

여기 있는 것은 **블랙홀 렌더러를 만들 때 쓴 프롬프트**입니다.
영상 편집(격자·컷·자막·음악)에 쓴 지시는 이 저장소 범위가 아니라 **빼 두었습니다.**

한 번에 다 주지 마세요. 단계마다 산출물을 보고 다음을 주는 게 핵심입니다 —
뒤 단계에서 확정될 값을 앞 단계가 추측으로 채우면 조용히 틀립니다.

각 STEP 끝의 ✅ 는 **그 단계에서 눈으로 확인할 것**입니다.

---

## STEP 1 — 렌징 셰이더

```
WebGL2 프래그먼트 셰이더로 슈바르츠실트 블랙홀을 그려 주세요.

· 픽셀마다 카메라에서 광선을 거꾸로 쏩니다(backward ray tracing).
· 갱신식은 acc = -1.5 · h² · pos / r⁵ (h = |pos × vel|), 320스텝 적분입니다.
· 적도면(y=0)을 지날 때마다 강착원반 방출을 누적하세요 —
  한 광선이 여러 번 만나야 블랙홀 뒤쪽 원반이 감겨 보입니다(2차상).
· 밝기는 도플러 빔잉 dopp³ 와 중력 감쇠 √(1−Rs/r) 를 곱합니다.
  ★색을 바꾸는 것은 도플러뿐입니다. 중력항은 밝기만 깎으세요.
· 카메라(incl·az·zoom·fov)와 시간 t 는 전부 uniform 으로 받습니다.
· rAF · Date.now · Math.random 금지. 같은 uniform 이면 같은 픽셀이어야 합니다.
```
✅ 슬라이더로 아무 값이나 돌려 보고, **같은 값으로 돌아오면 같은 그림**인지 보세요.
   되돌렸는데 조금이라도 다르면 어딘가에 시간·난수가 들어간 것입니다.

## STEP 2 — 같은 식의 JS 판본

```
셰이더와 **같은 상수·같은 갱신식**으로 도는 JS 측지선 적분기를 만들어 주세요.
반환은 경로 점 배열 + 포획 여부 + 최소 반경 + 적도면 교차점 목록입니다.
상수(RS·DISK_IN·DISK_OUT·STEPS·DT)는 **한쪽에서만 정의**하고 다른 쪽이 가져다 쓰게 하세요.
```
✅ 두 벌이 되는 순간이 위험합니다. 상수를 양쪽에 적어 두면 언젠가 한쪽만 고쳐지고,
   그때부터 화면이 거짓말을 합니다.

## STEP 3 — 코드가 스스로 검사하게

```
이 렌더러가 일반상대론과 맞는지 검사하는 스크립트를 만들어 주세요. 브라우저 없이 node 로 돕니다.

1. 결정론 — 같은 입력으로 두 번 적분하면 비트까지 같은가
2. 약장 편향각이 4M/b 로 수렴하는가 (2차항 15πM²/4b² 까지 대조할 것)
3. 포획 경계(임계 임팩트 파라미터)가 3√3·M 인가 — 이분법으로 실제로 찾아서 비교
4. 화면 좌표로 그린 그 원의 안쪽은 포획되고 바깥은 탈출하는가
5. 적도면을 2회 지나는 광선이 실재하는가, 그게 그림자 아래쪽에 모이는가
6. 같은 반경의 원반 좌우에서 도플러 빔잉 배수가 얼마나 갈리는가

각 검사는 **무엇을 검증하는지** 한 줄로 같이 출력하세요.
```
✅ ③에서 **이론값을 넣지 말고 이분법으로 찾게** 하세요. 상수를 적어 두고 그 상수와 비교하면
   검사가 아니라 동어반복입니다.

## STEP 4 — 화면 좌표 ↔ 임팩트 파라미터

```
그림자 경계를 화면에 원으로 그리려면 반경이 몇 픽셀인지 알려 주세요.
카메라가 무한히 멀지 않다는 점을 반영해야 합니다.
```
✅ **`b/R` 로 선형 근사하면 원이 그림자 안쪽으로 들어갑니다.**
   화면 좌표 → 방향이 `normalize(fwd + fov·u)` 이므로 각도가 `atan(fov·u)` 이고,
   임팩트 파라미터는 `b = |cam × dir| = R·sinθ` 입니다.
   ⇒ `u = tan(asin(b/R)) / fov`. 실측에서 선형 근사와 2.5px 차이였는데,
   그 2.5px 안쪽 광선은 **실제로 포획**됐습니다. 2px 때문에 주장이 거짓이 됩니다.

## STEP 5 — 정지 프레임 굽기

```
playwright 로 임의 각도의 정지 프레임을 PNG 로 굽는 스크립트를 주세요.
같은 인자면 같은 PNG 가 나와야 하고, 그걸 확인하는 --check 옵션도 넣어 주세요
(한 번 굽고 → 다른 값으로 흔들고 → 다시 굽어서 픽셀 SHA 를 비교).
```
✅ **실 GPU 에서는 통과 못 할 수 있습니다.** 같은 프레임을 순서만 바꿔 그렸더니
   RTX 3060 / ANGLE D3D11 에서 최대 Δ8/255 가 수백 픽셀에 흩어졌습니다.
   결정론이 필요하면 SwiftShader(`--use-angle=swiftshader`)로 도세요. 25배 느리지만 재현됩니다.

---

## 참고 — 예시 이미지를 만든 프롬프트 (FLUX.1 schnell)

영상 앞부분에 「생성 모델은 같은 요청에도 매번 다른 그림을 준다」를 보여주려고 쓴 두 장입니다.
**같은 시드(20260801)에 각도 단어만** 바꿨습니다.

```
a black hole with a glowing orange accretion disk in deep space,
seen from directly above, looking down on the disk from a high angle,
cinematic astrophotography, hot plasma, dark starfield background
```
```
a black hole with a glowing orange accretion disk in deep space,
seen from below, looking up at the underside of the disk from a low angle,
cinematic astrophotography, hot plasma, dark starfield background
```
`FLUX.1 schnell` (GGUF Q4_K_S) · steps 4 · cfg 1.0 · euler/simple · 1024×768.

⚠**이 두 장은 이 저장소에 넣지 않았습니다.** 생성 모델 출력은 시드를 줘도 구현체·버전에 따라
달라질 수 있어서, 파일을 올리면 「이 프롬프트의 결과」라는 주장이 언젠가 거짓이 됩니다.
프롬프트와 파라미터만 남깁니다 — 직접 돌려 보세요.
