# 이 영상을 AI에게 처음부터 다시 시키는 순서

아래 STEP 을 순서대로 던지면 됩니다. **한 번에 다 주지 마세요** — 단계마다 산출물을 보고
다음을 주는 게 이 파이프의 핵심입니다(값이 뒤 단계에서 확정되는데 앞 단계가 추측으로 채우면 사고가 납니다).

각 STEP 끝의 ✅ 는 **그 단계에서 눈으로 확인할 것**입니다.

---

## STEP 0 — 격자부터 정한다 (길이는 고르는 게 아니라 풀린다)

```
세로 1080×1920 쇼츠를 만들 겁니다. 컴프(장면)를 2~3초마다 바꾸고 싶어요.
25fps 에서 컴프 길이가 정수 프레임이고, 그 길이가 정수 박이 되는 템포를 구해 주세요.
총 길이는 정수 마디여야 합니다(BGM 이음매가 0 이 되게).
```
✅ 25fps·2~3초·정수 박 → **BPM 120**(2.0s=4박=50f · 3.0s=6박=75f)이 유일해로 나와야 합니다.
   50f 와 75f 를 섞어 750f(=30.000s=15마디)를 만드세요. **2-3-2-3 으로 번갈아 놓지 마세요**
   — 그 자체가 예측 가능한 맥박이 됩니다.

## STEP 1 — 시선 흐름을 컴프보다 먼저 그린다

```
컴프마다 「진입 앵커(px)」와 「종점(px)」을 먼저 정해 표로 주세요. 규칙:
 · 새 정보가 차갑게 처음 놓이는 자리는 좌상 > 우상 > 좌하 > 우하
 · 컴프 k+1 의 진입 앵커 = 컴프 k 의 종점 (320px 이내)
 · 한 막 안에서 시선 이동은 단조. 되돌아오는 이동 금지
 · 단 이 우선순위를 막마다 기계적으로 반복하지 말 것 — 막마다 traversal 모양을 다르게
```
✅ 표만 보고도 눈이 어디서 어디로 가는지 읽혀야 합니다. 이 표는 나중에 **게이트(G8)로 검사**합니다.

## STEP 2 — 렌징 셰이더

```
WebGL2 프래그먼트 셰이더로 슈바르츠실트 블랙홀을 그려 주세요.
픽셀마다 카메라에서 광선을 거꾸로 쏘고, acc = -1.5·h²·pos/r⁵ 를 320스텝 적분합니다(h = |pos×vel|).
적도면(y=0)을 지날 때마다 강착원반 방출을 누적하세요 — 한 광선이 여러 번 만나야 2차상이 생깁니다.
도플러 빔잉은 dopp³, 중력 감쇠는 √(1−Rs/r) 로 밝기에만 겁니다(색은 도플러만 바꿉니다).
시간은 uniform 으로만 받습니다. rAF·Date.now·Math.random 금지.
```
✅ `?preview` 로 열어 아무 프레임이나 눌러 보세요. **같은 프레임은 항상 같은 그림**이어야 합니다.

## STEP 3 — 같은 적분식의 JS 판본

```
셰이더와 **같은 상수·같은 갱신식**으로 도는 JS 측지선 적분기를 만들어 주세요.
반환은 경로 점 배열 + 포획 여부 + 최소 반경 + 적도면 교차점 목록입니다.
화면에 광선을 선으로 그리고, 검증 스크립트가 이걸 씁니다.
```
✅ 여기서 두 벌이 되는데, **상수를 한쪽에서만 받아 쓰게** 하세요. 갈리면 화면이 거짓이 됩니다.

## STEP 4 — 인폴은 셰이더가 아니다

```
빨려드는 별을 속도 Verlet 으로 사전 적분해 주세요. 중력 -GM·r/|r|³ + 항력 -k(rRef/r)^q·v.
항력을 **반경 의존**으로 두세요 — 균일 항력이면 r(t)=r₀e^(−2kt) 라 모든 별이 같은 비율로 줄어
한꺼번에 사라집니다. 초기 반경은 균등 분포로(면적가중 √ 는 바깥에 몰려 헤일로가 됩니다).
```
✅ 「몇 프레임에 몇 개가 살아 있고 반경 분포가 어떤지」를 **먼저 찍어** 보세요.
   그림이 아니라 숫자로 고르는 자리입니다.

## STEP 5 — 화면에 뜨는 코드는 지어낼 수 없게

```
화면에 소스 코드 일부를 띄울 겁니다. 앵커 문자열로 원문에서 **연속 구간**을 뽑아
실제 줄번호와 함께 굽는 스크립트를 만들어 주세요. 앵커가 0회 또는 2회 이상이면 빌드 실패.
원문 sha256 을 각인하고, 렌더러가 대조해 어긋나면 **렌더를 거부**하게 하세요.
```
✅ 줄번호를 손으로 쓰지 마세요. 사람이 쓰면 틀립니다(그리고 아무도 안 봅니다).

## STEP 6 — 결정론 게이트를 먼저 만든다

```
컴프 경계·중앙 프레임을 정순으로 찍고 **역순으로 다시 찍어** 픽셀 SHA 를 대조하는 게이트를
만들어 주세요. 하나라도 다르면 실패로 종료합니다.
```
✅ 여기서 걸리는 것들:
   · **캔버스 2D 상태 누수** — 앞 프레임이 남긴 `lineCap` 하나가 다음 프레임을 12픽셀 바꿉니다.
     프레임 시작마다 컨텍스트를 표준 상태로 되돌리세요.
   · **`shadowBlur`** — 결정론이 아닙니다. 글자는 어두운 외곽선, 점은 방사 그라디언트,
     선은 넓은 반투명 밑선으로 바꾸세요.
   · **GPU** — 실 GPU 는 같은 프레임을 Δ4~8/255 로 다르게 그립니다. 납품은 SwiftShader 로.

## STEP 7 — 「완결 전에 끊기지 않게」를 잰다

```
컴프의 상시 모션(공전·궤도·자막)을 고정한 뒤, 각 프레임을 그 컴프의 마지막 프레임과 비교해
언제부터 잠잠해지는지 재 주세요. 잠잠해진 시점부터 컴프 끝까지가 유지 시간이고 하한은 0.40초입니다.
```
✅ 상시 모션을 안 고정하면 **지표가 오염**됩니다. 「변해도 되는 것」을 먼저 정의하세요.

## STEP 8 — 나레이션·BGM·믹스

```
나레이션 6줄을 배치하는데, 전부 컴프 경계에서 시작하게 하고 assert 로 막아 주세요.
라인 사이 「들리는 간격」은 파일 길이 − 말단 페이드 − 패딩으로 재고 하한 0.30초입니다.
믹스는 BGM:나레 = 10:90 에너지 지분 · 덕킹 0 · 정적 게인 + 피크 삭감으로 −14 LUFS 에 착지시키세요.
```
✅ ASR 어절 끝은 실제 소리 끝보다 0.3~0.4초 이릅니다. 그걸로 간격을 재면 **5배 틀립니다**.

## STEP 9 — 검증 스크립트

```
영상이 화면에서 한 주장마다 그게 코드에서 참인지 검사하는 스크립트를 만들어 주세요.
편향각이 4M/b 에 수렴하는가, 포획 경계가 3√3·M 인가, 적도면 2회 통과 광선이 실재하는가,
다가오는 쪽이 밝은가, 항력 0 이면 각운동량이 보존되는가.
```
✅ 1차항만으로 판정하면 b 가 작을 때 「엔진이 틀렸다」는 잘못된 결론이 납니다 —
   2차항까지 넣어 대조하고, **판정 구간을 명시**하세요.

## STEP 10 — 재현 패키지

```
작업 트리 밖에서도 그대로 도는 저장소로 묶어 주세요. 화면이 소스의 실제 줄을 띄우고 있으므로
**경로를 고치지 말고 배치로** 푸세요(소스는 src/, 폰트는 저장소 루트 _fonts/).
```
✅ 마지막에 **패키지에서 구운 프레임과 작업본 프레임의 PNG sha256 을 대조**하세요.
   같지 않으면 「재현됩니다」는 거짓말입니다.
